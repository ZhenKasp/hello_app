<h1>All posts</h1>
<% @post.each do |post| %>
  <div class="alert alert-dark">
    <h3><%= post.title %></h3>
    <post text-overflow="ellipis" ><%= simple_format(post.body) %></post>
    <%= link_to "Read more", post_path(post), :class => "btn btn-outline-primary" %>
  </div>
  <br>
<% end %>
